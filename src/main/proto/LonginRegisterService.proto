syntax="proto3";
package employee;
import "Login.proto";
import "SignUp.proto";

service LoginLogoutService{
  rpc login(Login) returns (LoginResponse);
  rpc register( SignUp) returns (RegisterResponse);
}